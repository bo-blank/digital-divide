---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className = '', narrow = false } = Astro.props;
const maxWidth = narrow ? 'max-w-3xl' : 'max-w-6xl';
---

<div class:list={['mx-auto px-4', maxWidth, className]}>
  <slot />
</div>
